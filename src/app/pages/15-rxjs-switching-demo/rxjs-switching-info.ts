export interface FlatteningMoment {
  id: 'compare' | 'switchMap' | 'mergeMap' | 'concatMap' | 'exhaustMap';
  title: string;
  desc: string;
  marble: string;
  code: string;
  why: string;
}

export const SWITCHING_ESSENTIALS = [
  'Flattening-операторы разворачивают «поток потоков»: внешнее значение → внутренний Observable (обычно HTTP).',
  'switchMap — отменяет предыдущий внутренний поток. Для живого поиска / автокомплита.',
  'mergeMap — запускает все параллельно без отмены. Для независимых задач, где важна пропускная способность.',
  'concatMap — строгая очередь по одному. Для последовательной записи (POST/PUT), где важен порядок.',
  'exhaustMap — игнорирует новые, пока идёт текущий. Для защиты от двойного клика (Submit / login).',
];

export const SWITCHING_MOMENTS: FlatteningMoment[] = [
  {
    id: 'compare',
    title: '0. Живое сравнение четырёх операторов',
    desc: 'Один и тот же поток кликов прогоняется через switchMap, mergeMap, concatMap и exhaustMap. Каждый «клик» — это запрос ~1.3 с. Нажмите несколько раз подряд и сравните, какие запросы дойдут до done.',
    marble: `clicks ─1─2─3──────────►
switch  ────────────[3]──►  (1,2 отменены)
merge   ──────[1][2][3]──►  (все, как готовы)
concat  ──────[1]─[2]─[3]►  (по очереди)
exhaust ────────────[1]──►  (2,3 проигнорированы)`,
    code: `// Один источник — четыре стратегии разворачивания
clicks$.pipe(switchMap(id => req(id)))   // последний
clicks$.pipe(mergeMap(id => req(id)))    // все параллельно
clicks$.pipe(concatMap(id => req(id)))   // очередь
clicks$.pipe(exhaustMap(id => req(id)))  // первый, остальное игнор`,
    why: 'Выбор оператора = выбор стратегии конкуренции запросов. Это не «какой удобнее», а «что делать, когда новое событие пришло до завершения предыдущего»: отменить, распараллелить, поставить в очередь или проигнорировать.',
  },
  {
    id: 'switchMap',
    title: '1. switchMap — живой поиск',
    desc: 'На каждый новый ввод отменяет предыдущий незавершённый запрос и подписывается на новый. Гарантирует, что отрисуется результат именно последнего запроса — без гонок и устаревших ответов.',
    marble: `term  ─a──ab──abc────────►
        switchMap(search)
out   ──────────────[abc]►  (a, ab отменены)`,
    code: `results$ = this.term$.pipe(
  debounceTime(300),
  distinctUntilChanged(),
  switchMap(term => this.api.search(term)),  // старый запрос → unsubscribe → xhr.abort()
);`,
    why: 'Почему switchMap, а не mergeMap: при живом поиске ответ на «ab» не нужен, когда уже ввели «abc». switchMap отменяет устаревший запрос (в Angular это реально прерывает XHR), mergeMap бы оставил гонку и риск показать устаревший результат.',
  },
  {
    id: 'mergeMap',
    title: '2. mergeMap — параллельная обработка',
    desc: 'Запускает внутренние потоки для всех входящих значений одновременно и сливает результаты по мере готовности, ничего не отменяя. Максимальная пропускная способность.',
    marble: `ids   ─1─2─3────────────►
        mergeMap(load)
out   ─────[2][1][3]─────►  (в порядке готовности)`,
    code: `// Загрузить детали для каждого id — независимо и параллельно
details$ = from(ids).pipe(
  mergeMap(id => this.api.getDetails(id)),
  // при желании ограничить параллелизм: mergeMap(fn, 4)
);`,
    why: 'Почему mergeMap, а не concatMap: задачи независимы и порядок неважен — нет смысла ждать друг друга. concatMap здесь искусственно замедлил бы загрузку, выполняя запросы по одному. Параметр concurrency у mergeMap ограничивает число одновременных запросов.',
  },
  {
    id: 'concatMap',
    title: '3. concatMap — последовательная запись',
    desc: 'Ставит внутренние потоки в очередь: следующий стартует только после complete предыдущего. Сохраняет порядок — критично для последовательных мутаций на сервере.',
    marble: `ops   ─A─B─C────────────►
        concatMap(save)
out   ──[A]──[B]──[C]────►  (строго по порядку)`,
    code: `// Очередь сохранений: каждый PUT уходит только после ответа на предыдущий
saved$ = this.edits$.pipe(
  concatMap(edit => this.api.save(edit)),  // порядок гарантирован
);`,
    why: 'Почему concatMap, а не mergeMap: при параллельной отправке порядок ответов не гарантирован — два PUT могут перезаписать друг друга в неверном порядке (потеря данных). concatMap сериализует запись. Минус — задержка при медленных запросах (растёт очередь).',
  },
  {
    id: 'exhaustMap',
    title: '4. exhaustMap — защита от двойного клика',
    desc: 'Пока выполняется текущий внутренний поток, все новые значения ИГНОРИРУЮТСЯ. Идеально для Submit/Login: повторные клики во время запроса не плодят дубликаты.',
    marble: `clicks ─x─x─x──────x─────►
        exhaustMap(submit)
out   ──[x]────────[x]────►  (клики во время запроса игнор)`,
    code: `// Логин: второй клик во время запроса просто игнорируется
loginResult$ = this.loginClicks$.pipe(
  exhaustMap(() => this.auth.login(this.form.value)),  // пока идёт — новые клики мимо
);`,
    why: 'Почему exhaustMap, а не switchMap: switchMap отменил бы первый запрос и стартовал новый на каждый клик — пользователь долбит кнопку, а логин всё перезапускается. exhaustMap, наоборот, доводит первый до конца и глушит дребезг кликов.',
  },
];

export { flattenCurriculumToNavItems, type DemoNavItem } from '../curriculum/curriculum';

import { flattenCurriculumToNavItems } from '../curriculum/curriculum';

export const DEMO_NAV_ITEMS = flattenCurriculumToNavItems();
